---
/** @deprecated Use SiteSettings.astro */
import SiteSettings from './SiteSettings.astro';

interface Props {
	class?: string;
}

const { class: className = '' } = Astro.props;
---

<SiteSettings class={className} />
